import { Button } from "@material-tailwind/react";
import React from "react";

export default function AddButton() {
    return (
        <Button className="bg-primary py-2">
            Add
        </Button>
    )
}